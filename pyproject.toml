[project]
name = "chunked_array_set"
version = '0.0.1'

dependencies = [
    "numpy",
    "pandas",
    "pytest",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

